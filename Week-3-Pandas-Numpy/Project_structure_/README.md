project-name/
│
├── main.py           # Entry point (run the program)
├── operations.py     # Core business logic - add, substract, multiply, divide
├── utils.py          # Helper / reusable functions (validate input)
│
├── .env              # Environment variables (API keys, secrets)
├── .env.example      # Sample env file (recommended)
├── requirements.txt  # Dependencies
├── .gitignore        # Ignore venv, .env, __pycache__
└── README.md         # Project description & usage


# Simple rules - 
    - main.py -> no logic
    - operations.py -> what your app does
    - utils.py -> helpers only
    - .env -> secrets
    - README -> how to run

* Step-1: Install dependencies -  *
    pip install -r requirements.txt# Pandas_primer
